# Local profile - use Caffeine in-memory cache
spring.cache.type=caffeine
# No external Redis required for local development
# Use: set -Dspring.profiles.active=local or pass --spring.profiles.active=local
